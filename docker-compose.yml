version: "3.8"

services:
  backend:
    build:
      context: ./backend
    container_name: rndr-backend
    ports:
      - "3000:3000"
    restart: always

  frontend:
    build:
      context: ./frontend
    container_name: rndr-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: always

